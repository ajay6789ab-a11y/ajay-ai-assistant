{
  "name": "kirana-store-manager",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Free, offline-first Kirana (grocery) store management PWA \u2014 HTML/CSS/Vanilla JS + IndexedDB. No paid APIs, no paid database, no subscription.",
  "license": "MIT",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "serve": "python3 -m http.server 8080 --bind 0.0.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "jsdom": "^30.1.0"
  }
}
